numpy
pandas
Pillow
scipy
scikit-image   # skeletonize (thinning, morphology); fallback erosion used if missing
scikit-learn   # LinearRegression for axis calibration (pixel -> physical value mapping)

# Dev / testing
pytest

# Optional: ML-assisted candidate reranking
# torch

# OCR for axis tick value reading (auto-detect)
pytesseract     # requires: brew install tesseract
# easyocr       # optional fallback (heavy ~1GB), uncomment if needed
