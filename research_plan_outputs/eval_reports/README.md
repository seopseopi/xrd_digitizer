# 이 폴더의 eval_reports JSON에 대해

여기 저장된 `report_*_*.json`은 **진단 스크립트가 B1 참조 배치**(`outputs/0504/runs/eval_ablation_08_ridge_m4_peak1`)에 대해 생성한 것이다.

- **공식 모델 도입 baseline(B0)이 아니다.**
- 모델 도입 여부는 `dist/xrd_digitizer_model_v1_3` 기준으로 새로 생성한 리포트와 M1을 비교해야 한다 (`ml/model_integration_compare.py`).
